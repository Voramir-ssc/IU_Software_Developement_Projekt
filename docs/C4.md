# Softwarearchitektur: Familien Hero (C4-Modell Level 2)

Dieses Dokument visualisiert das Container-Diagramm (Level 2) der "Familien Hero"-Anwendung nach der offiziellen C4-Notation, optimiert für eine saubere Darstellung.

## Container-Diagramm

```mermaid
flowchart TD
    %% Custom C4 Styles für sauberes Rendering
    classDef person fill:#1e3a8a,stroke:#1e40af,color:#f8fafc,rx:8px,ry:8px
    classDef container fill:#0ea5e9,stroke:#0284c7,color:#f8fafc,rx:8px,ry:8px
    classDef database fill:#0ea5e9,stroke:#0284c7,color:#f8fafc
    classDef boundary fill:transparent,stroke:#94a3b8,stroke-width:2px,stroke-dasharray: 5 5,color:#f8fafc

    User["fa:fa-user Familien-Mitglieder\n[Person]\n\nEltern (Admins) und Kinder (Helden),\ndie den familiären Alltag organisieren."] ::: person

    subgraph SystemBoundary ["Familien Hero [System]"]
        direction TB
        SPA["Web Application (SPA)\n[Container: React.js, Vite, TypeScript]\n\nBietet die Gamification-Oberfläche und steuert\ndas clientseitige State-Management (Hero-Switch)."] ::: container
        
        API["REST API Server\n[Container: Node.js, Express, TypeScript]\n\nVerarbeitet die zentrale Geschäftslogik (Punktevergabe)\nund validiert sämtliche Client-Anfragen."] ::: container
        
        DB[("Datenspeicher\n[Container: MongoDB]\n\nSpeichert Nutzerprofile, Missionen (Aufgaben)\nund den Belohnungskatalog persistent ab.")] ::: database
    end
    class SystemBoundary boundary

    User -- "Interagiert mit\n[HTTPS]" --> SPA
    SPA -- "Sendet API-Anfragen an\n[JSON / HTTPS]" --> API
    API -- "Liest und schreibt Daten\n[Mongoose Queries / TCP]" --> DB